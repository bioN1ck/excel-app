import { ToolbarState } from '@models/store.model';

export const DEFAULT_TITLE = 'New table';

export const DEFAULT_STYLES: ToolbarState = {
  textAlign: 'left',
  fontWeight: 'normal',
  textDecoration: 'none',
  fontStyle: 'normal',
};
